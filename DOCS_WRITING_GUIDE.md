# BangNiCMS 文档写作准则

**核心读者**：站点运营 / 内容编辑 / 翻译审核员，**不会写代码**。

文档要让一位 30 岁的市场经理在 5 分钟内完成具体操作，而不是让一位程序员理解系统架构。

---

## ✅ 必须写

1. **我点哪里** — 后台菜单路径、按钮名称（与界面 1:1 对应）
2. **弹出什么** — 弹窗 / 抽屉 / 表单的字段截图或文字描述
3. **填什么** — 必填项、典型值、不能填什么
4. **结果是什么** — 操作后前台 / 后台立即变化
5. **出错怎么办** — 常见报错的中文文案 + 应对动作
6. **业务后果** — "这一步会让前台 100 个产品页 404"，而不是"会触发级联清理事务"

## ❌ 不要写

1. ❌ **API 路径**：`POST /languages/:code/duplicate-from/:sourceCode`
   - 如必须暗示底层调用 → 改为"系统会自动同步翻译副本"
2. ❌ **数据库字段名**：`Language.urlSegmentRule` / `localeStatus = 'pending_translation'`
   - 改为"URL 段规则" / "状态：待翻译"
3. ❌ **代码片段 / SQL / JSON 返回示例**
   - 运营看到 `{}` 就关页面了
4. ❌ **类型表**（字段名 / 类型 / 必填三列）
   - 改为带说明的 bullet 列表
5. ❌ **性能讨论 / 事务保证 / 索引建议**
6. ❌ **状态机图 ASCII 流程图**
   - 改为"步骤 1 → 步骤 2 → 步骤 3"的有序列表
7. ❌ **"BCP 47" / "Prisma" / "Multipart" 等专业术语**
   - 改为"标准语言代码（如 en-US、zh-CN）"

## 写作语气

- **第二人称**："你点击..."、"你会看到..."
- **动词开头**："新建一种语言"，而不是"语言的新建机制"
- **现在时**："系统弹出..."，而不是"将会弹出"
- **避免被动语态**："系统校验" 而不是"会被校验"

## 内容结构（每篇）

1. **能解决什么问题**（1 段，2-3 句）
2. **去哪里操作**（菜单路径，加粗）
3. **核心步骤**（编号步骤 + 截图描述）
4. **常见场景**（2-4 个具体业务场景，每个 1 段）
5. **常见疑问 FAQ**（5-10 条短问答）
6. **下一步**（推荐继续阅读）

## 视觉元素

- **Steps 组件**：用于"按顺序做这些"的步骤
- **Aside `tip`**：实用建议、最佳实践
- **Aside `caution`**：操作前必看的警告
- **Aside `note`**：背景说明、可跳过
- **表格**：仅用于"对比 A 与 B"的场景，不要用于罗列字段

## 关键转换示例

### Bad（技术风）
> `Language` 实体的 `urlSegmentRule` 字段控制该语言在 URL 中的"段"。默认值 `/{code}/`，由 `LanguagesService.createLanguage` 创建时写入。BCP 47 校验通过 `isValidLanguageCode` 工具函数。

### Good（运营风）
> 新增语言时，"URL 段"默认是语言代码（如 `/en-US/`）。如果想让 URL 短一点，可以改成 `/en/`。系统会自动检查代码格式，错的会有红字提示。

### Bad
> 后端 `POST /languages/:code/duplicate-from/:sourceCode` 接口会复制源语言下所有 `*_locale` 表的记录，统一标记 `localeStatus = 'pending_translation'`。

### Good
> 在「语言管理」页选中新加的语言（如英文），点 **从默认语言复制翻译** 按钮，系统会立即给所有页面、产品、文章生成英文版"草稿"——内容暂时还是中文（占位），状态显示为"待翻译"。这一步是为了让后续 AI 翻译有起点。

---

## 自检清单（每写完一篇）

- [ ] 第一段说明这篇能帮运营干什么（不是"介绍 X 实体"）
- [ ] 没有任何 `api/path` 出现
- [ ] 没有 `字段名: 类型` 这种程序员表格
- [ ] 没有 JSON / SQL / 代码块（除非是 URL 示例）
- [ ] 每个操作都有明确的菜单路径
- [ ] 每个步骤都能让运营在系统里"按图索骥"完成
- [ ] FAQ 至少 5 条，且都是运营会问的（不是开发者会问的）
